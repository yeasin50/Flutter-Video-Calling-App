import 'package:flutter/material.dart';

class EmailTextField extends StatelessWidget {
  final double widht;
  final double height;

  const EmailTextField({Key key, this.widht, this.height}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("A"),
    );
  }
}
